---
kind: pipeline
type: docker
name: linux-amd64

platform:
  arch: amd64
  os: linux

steps:
- name: Ndase Mumet
  image: brock5555/recovery
  environment:
    TG_TOKEN:
      from_secret: TG_TOKEN
    TG_CHAT_ID:
      from_secret: TG_CHAT_ID
  commands:
  - bash setup.sh
  - bash sync.sh
  - bash build.sh
